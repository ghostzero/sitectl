[package]
name = "sitectl"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sitectl"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive", "color"] }
colored = "2"
anyhow = "1"
walkdir = "2"
libc = "0.2"
